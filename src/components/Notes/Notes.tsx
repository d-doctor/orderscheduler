import React from 'react';
import { Data } from '../../interfaces/VendorModels';

interface Props {
  orderItem: Data;
}
function Notes({ orderItem }: Props) {
  return <></>;
}

export default Notes;
